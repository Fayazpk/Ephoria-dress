<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Categories - Euphoria Dress</title>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
  <%= stylesheet_link_tag 'application', media: 'all' %>
  <%= stylesheet_link_tag 'categories', media: 'all' %>
</head>
<body>
  <!-- Hero Section -->
  <header class="hero">
    <div class="container">
      <div class="hero-content">
        <div class="hero-text">
          <h1>Redefine Your Style, <span>Embrace Your Elegance</span></h1>
          <p>Curated dress collections that tell your unique story. Discover fashion that speaks volumes.</p>
          <div class="hero-buttons">
            <a href="#" class="btn-primary">Shop Now</a>
            <a href="#collections" class="btn-secondary">Explore Collections</a>
          </div>
        </div>
        <div class="hero-image">
          <div class="image-card">
            <div class="image-overlay"></div>
            <!-- Hero image will be set as background in CSS -->
          </div>
        </div>
      </div>
    </div>
  </header>

  <!-- Featured Collections -->
  <section id="collections" class="featured-collections">
    <div class="container">
      <div class="section-header">
        <h2>Featured Collections</h2>
        <p>Discover our curated selections for every occasion</p>
      </div>
      
      <div class="collections-grid">
        <% @categories.each do |category| %>
          <%= link_to usermodule_category_subcategories_path(category), class: "category-card" do %>
            <div class="card-image">
              <% if category.image.attached? %>
                <%= image_tag category.image, alt: category.name %>
              <% else %>
                <div class="placeholder-image">
                  <i class="fas fa-images"></i>
                </div>
              <% end %>
              <div class="card-overlay">
                <span class="explore-text">Explore Collection</span>
              </div>
            </div>
            <div class="card-content">
              <h3><%= category.name %></h3>
              <p><%= category.description %></p>
            </div>
          <% end %>
        <% end %>
      </div>
    </div>
  </section>

  <!-- New Arrivals -->
  <section class="new-arrivals">
    <div class="container">
      <div class="section-header">
        <h2>New Arrivals</h2>
        <p>The latest additions to our exclusive collection</p>
      </div>
      
      <div class="products-slider">
        <% @product.each do |product| %>
          <div class="product-card">
            <div class="product-image">
              <% if product.images.attached? %>
                <%= image_tag product.images.first, alt: product.name %>
              <% else %>
                <div class="placeholder-image">
                  <i class="fas fa-tshirt"></i>
                </div>
              <% end %>
              <div class="product-actions">
                <button class="action-btn wishlist-btn">
                  <i class="far fa-heart"></i>
                </button>
                <button class="action-btn cart-btn">
                  <i class="fas fa-shopping-bag"></i>
                </button>
                <button class="action-btn view-btn">
                  <i class="far fa-eye"></i>
                </button>
              </div>
            </div>
            
            <div class="product-content">
              <h3><%= product.name %></h3>
              <p class="product-description"><%= truncate(product.description, length: 60) %></p>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </section>

  <!-- Trending Section -->
  <section class="trending-section">
    <div class="container">
      <div class="trending-content">
        <div class="trending-text">
          <span class="subtitle">SEASONAL TRENDS</span>
          <h2>Elevate Your Summer Style</h2>
          <p>Discover our curated collection of summer essentials designed to keep you stylish during the hottest days.</p>
          <a href="#" class="btn-outline">Explore Trends</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Newsletter -->
  <section class="newsletter">
    <div class="container">
      <div class="newsletter-content">
        <h3>Subscribe to Our Newsletter</h3>
        <p>Get exclusive offers, new arrival alerts, and style inspiration directly to your inbox.</p>
        <form class="newsletter-form">
          <input type="email" placeholder="Your Email Address" required>
          <button type="submit">Subscribe</button>
        </form>
      </div>
    </div>
  </section>

  <!-- JavaScript -->
  <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
</body>
</html>